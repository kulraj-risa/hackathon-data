# Data access (needed for data_loader.py / Queries 1-3)
pandas>=2.2
google-cloud-bigquery>=3.25
db-dtypes>=1.2
pyarrow>=16.0
python-dotenv>=1.0

# Core ML (used in later steps: feature_engineer / model_trainer)
numpy>=1.26
scikit-learn>=1.4
xgboost>=2.0
shap>=0.45
joblib>=1.3

# Web app / serving (FastAPI, no Streamlit)
fastapi>=0.110
uvicorn[standard]>=0.29
