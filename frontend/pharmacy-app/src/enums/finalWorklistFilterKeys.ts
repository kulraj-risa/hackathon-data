export enum FinalWorklistFilterKeys {
  CreatedAt = "created_at",
  DateOfService = "date_of_service",
  DrugName = "active",
  OrderStatus = "auth_status",
  UploadDate = "upload_timestamp",
}

export enum FinalWorklistOrderStatus {
  Approved = "approved",
  Denied = "denied",
  Pending = "pending",
  InProgress = "in_progress",
}
