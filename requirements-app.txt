# Runtime deps for the hosted API (Cloud Run container).
# Serves precomputed de-identified data + runs the trained XGBoost model.
fastapi>=0.110
uvicorn[standard]>=0.29
pandas>=2.2
pyarrow>=16.0
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
xgboost>=2.0
joblib>=1.3
# Audit/prediction store when STORAGE_BACKEND=firestore (its own isolated DB).
google-cloud-firestore>=2.16
